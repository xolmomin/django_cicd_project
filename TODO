ssh-key generate qilish

ssh-keygen -t rsa -b 4096 -C "test@gmail.com"

ssh-keygen -t ed25519 -a 200 -C "your_email@example.com"

/etc/nginx/sites-enabled (nginx)
/etc/systemd/system/service_nomi (service)

systemctl enable service_nomi
systemctl start service_nomi
systemctl status service_nomi
systemctl restart service_nomi

Termius

username: root
password: ${{ secrets.PASSWORD }}
key: ${{ secrets.KEY }}
script: |
  echo 'hello'
  touch test.txt
  whoami

change token




/var/www/


1martalik job
git clone
.venv
nginx.conf
.service


git pull
pip3 install req.txt
restart service, nginx


ssh -T git@github.com
